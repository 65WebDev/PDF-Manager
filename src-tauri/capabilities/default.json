{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for PDF Manager desktop shell",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "allow-take-pending-open-files",
    "allow-read-local-file"
  ]
}
