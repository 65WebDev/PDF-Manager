{
  "name": "pdf-manager",
  "private": true,
  "type": "module",
  "scripts": {
    "build:offline": "node scripts/build-offline.mjs",
    "build:tauri-ui": "node scripts/prepare-tauri-ui.mjs",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build && node scripts/upload-windows-installer.mjs",
    "tauri:build:local": "tauri build",
    "tauri:upload": "node scripts/upload-windows-installer.mjs",
    "tauri:bump-if-needed": "node scripts/bump-windows-version-if-needed.mjs"
  },
  "devDependencies": {
    "@cantoo/pdf-lib": "2.7.4",
    "@tauri-apps/cli": "^2.11.4",
    "esbuild": "^0.25.0"
  },
  "dependencies": {
    "@kenjiuno/decompressrtf": "^0.1.4",
    "@kenjiuno/msgreader-web-ng": "^0.2.0-alpha1",
    "@pdf-lib/fontkit": "^1.1.1",
    "postal-mime": "^2.7.5"
  }
}
